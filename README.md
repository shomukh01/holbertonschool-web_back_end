# holbertonschool-web_back_end

Holberton School backend projects written in Python.

## Pagination

The `pagination` directory contains exercises for calculating page ranges
and serving paginated data from `Popular_Baby_Names.csv`.

